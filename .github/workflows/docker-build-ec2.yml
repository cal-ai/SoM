# Autogenerated via deploy.py, do not edit!

name: Docker Build on EC2 Instance

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: SSH and Execute Build on EC2
        uses: appleboy/ssh-action@master
        with:
          command_timeout: "60m"
          host: 3.144.154.220
          username: ubuntu  # Usually 'ubuntu' or 'ec2-user'
          
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          
          script: |
            source activate pytorch
            nvidia-smi

            rm -rf SoM || true
            git clone https://github.com/cal-ai/SoM
            cd SoM
            git checkout main
            git pull

            # Stop and remove existing container if it's running
            sudo docker stop cal-ai-container || true
            sudo docker rm cal-ai-container || true

            # Build the image
            sudo nvidia-docker build -t cal-ai . || exit 1

            # Run the image
            sudo docker run -d -p 6092:6092 --gpus all --name cal-ai-container \
              -e OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }} \
              cal-ai